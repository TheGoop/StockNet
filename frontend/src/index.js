import React from 'react'
import ReactDOM from 'react-dom'
import App from './App'

ReactDOM.render(<App />, document.querySelector('#root'));


//basic boiler plate code needed for every React application
//React will inject all of our code into the root in the root of the html   